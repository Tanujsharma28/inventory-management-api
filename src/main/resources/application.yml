spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_db
    username: root
    password: Tanuj@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  application:
    name: enterprise-inventory

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"